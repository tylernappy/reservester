<!DOCTYPE html>
<html>
<head>
  <title>Reservester</title>
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<%= nav_bar :fixed => :top, :brand => "Ty's Kickin' Restaurant Guide" do %>
    <% menu_group do %>
        <%= menu_item "Restaurants", root_path %>
        <% if current_owner %>
        	<%= menu_item "Logout", destroy_owner_session_path, method: :delete %>
        <% else %>
        	<%= menu_item "Login", new_owner_session_path %>
    	<% end %>
    <% end %>
<% end %>


<div class="container">
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

	<%= yield %>
</div>
</body>
</html>
